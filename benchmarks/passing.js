// Generated by CoffeeScript 1.10.0
(function() {
  var Benchmark, Indicative, Joi, correct_user_data, indicativeSchema, joiSchema, suite, validationPassedUsingIndicative, validationPassedUsingJoi, validator;

  Benchmark = require('benchmark');

  suite = new Benchmark.Suite;

  Indicative = require('../lib/validator');

  validator = new Indicative();

  correct_user_data = {
    username: 'john',
    email: 'john@example.com',
    password: 'johny'
  };

  Joi = require('joi');

  joiSchema = Joi.object().keys({
    username: Joi.required(),
    password: Joi.required(),
    email: Joi.string().email().required()
  });

  indicativeSchema = {
    username: 'required',
    password: 'required',
    email: 'required|email'
  };

  validationPassedUsingJoi = function() {
    return Joi.validate(correct_user_data, joiSchema, function(err, value) {});
  };

  validationPassedUsingIndicative = function() {
    return validator.validate(indicativeSchema, correct_user_data).then(function(success) {})["catch"](function(err) {});
  };

  suite.add('Validation Passed Using JOI', function() {
    return validationPassedUsingJoi();
  }).add('Validation Passed Using Indicative', function() {
    return validationPassedUsingIndicative();
  }).on('cycle', function(event) {
    return console.log(String(event.target));
  }).on('complete', function() {
    return console.log("Fastest is " + (this.filter('fastest').pluck('name')));
  }).run({
    'async': true
  });

}).call(this);
